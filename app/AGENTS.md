# Expo SDK 54 — NO SUBIR

Este proyecto se queda en **Expo SDK 54** (es lo que soporta el Expo Go instalado desde el App Store). No subir el SDK ni las versiones de dependencias sin una decisión explícita.

Docs versionadas: https://docs.expo.dev/versions/v54.0.0/
